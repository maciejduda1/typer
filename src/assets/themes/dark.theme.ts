import { createTheme } from "@mui/material";

const darkTheme = createTheme({
	palette: {
		mode: "dark",
		primary: {
			main: "#254f9d",
		},
		secondary: {
			main: "#9d7325",
		},
	},
});

export default darkTheme;
